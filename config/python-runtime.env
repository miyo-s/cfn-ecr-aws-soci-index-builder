# 我々 (fork) がデプロイしたい Python Lambda ランタイム。
# upstream の templates/SociIndexBuilder.yml は変更せず (merge conflict 回避)、
# CI/CD 実行時に scripts/apply-python-runtime.sh でこの値へ置換する。
# 値は AWS Lambda の runtime 識別子 (例: python3.13) をそのまま書く。
PYTHON_RUNTIME=python3.13
